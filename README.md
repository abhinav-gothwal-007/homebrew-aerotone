# homebrew-aerotone

Homebrew tap for [AeroTone](https://github.com/abhinav-gothwal-007/AeroTone).

```sh
brew install --cask abhinav-gothwal-007/aerotone/aerotone --no-quarantine
```

`--no-quarantine` is needed because the app is ad-hoc signed (not notarized).
